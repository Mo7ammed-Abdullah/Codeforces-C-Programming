#include <stdio.h>

int main(){
    int a;
double rest, n;
scanf("%lf" , &n);
a = n;
rest = n-a;

if(rest==0){
   printf("int %d" , a);
   }
   else{
    printf("float %d %.3lf" , a , rest);
   } 

return 0;
}
